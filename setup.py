from setuptools import setup, find_packages

setup(
    name="performance_test_decorator",
    version="1.0.0",
    description="A simple Python decorator for benchmarking function performance.",
    author="AMIRBAXXTER",
    packages=find_packages(),
    python_requires=">=3.6",
)
